<UserControl x:Class="WebCrawler.View.WebsiteTree.WebsiteTreeBranchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebCrawler.View.WebsiteTree"
             mc:Ignorable="d" Height="20" Width="800">
    <Grid>
 
        <TextBlock Margin="{Binding Indentation}">
            <Hyperlink  NavigateUri="{Binding PageUrl, Mode=OneWay}" RequestNavigate="Hyperlink_RequestNavigate" >
                <TextBlock Text="{Binding PageTitle, Mode=OneWay}"/>
            </Hyperlink>
        </TextBlock>
        
    </Grid>
</UserControl>
